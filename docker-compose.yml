version: '3'
services:

  db:
    image: postgres
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: book_db

  rustapi:
    build:
      context: server
      dockerfile: DockerFile
    ports:
      - "3000:3000"
